<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dd.dao.SkuDao">
    <select id="findHkmk" resultType="dd.mapper.Sku">
        select * from sku
    </select>

    <delete id="deleteId">
delete FROM goods where id = #{id}
    </delete>
</mapper>
 